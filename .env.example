PORT=8080
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/quiicast?retryWrites=true&w=majority
# Comma-separated list of allowed frontend origins for CORS / WS origin checks
ALLOWED_ORIGINS=http://localhost:4321,https://quiicast.com,https://www.quiicast.com
OTP_TTL_MINUTES=10
